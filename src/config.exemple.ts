// Replace config.exempke.ts to config.ts

interface Config {
  androidClientId:string;
  iosClientId:string;
}

const config:Config = {
  androidClientId: '',
  iosClientId: ''
}

export default config
